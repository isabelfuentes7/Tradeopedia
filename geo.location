<script>
        const successCallback = (position) => {
            console.log(position);
        };
        const errorCallback = (error) => {
            console.error(error);
        };
        const watchIDd = navigator.geolocation.getCurrentPosition(successCallback, errorCallback);
    </script>